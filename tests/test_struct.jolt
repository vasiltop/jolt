module tests;
import "./common.jolt";

struct Point {
    x: i32;
    y: i32;
}

fn main() i32 {
    let p = Point { x: 10, y: 20 };
    p.x = 30;
    
    common:assert(p.x == 30);
    common:assert(p.y == 20);
    
    ret 0;
}
