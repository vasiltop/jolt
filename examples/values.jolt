module values;
import "./math/constants.jolt";
import "./std/io.jolt";

fn main() i32 {
    let n = 42;
    let s = "ok";
    let c = 'z';
    let ok = true;
    let r = 1.5;
		let small: u8 = 53 as u8;
		let arr = [1, 2, 3, 4];
		let ind = arr[n];
		let m = constants:PI;
		let afds = true;

		let vector = constants:V3 {
				x: 0.0, y: 0.5, z: 5.0
		};

		io:printf("%d %d\n", vector.x, vector.y);

    ret n + r + 15;
}
