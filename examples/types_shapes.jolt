import math::geometry;

struct MyStruct {
    field1: i32;
    field2: *math::geometry::Vector;
    field3: **i64;
}

enum ShapeKind {
    Field1,
    Field2,
}

fn use_vec(val: *math::geometry::Vector) *i32 {
    let x: *i32 = null;
    let y = x as *i64;
    ret x;
}
